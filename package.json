{
  "name": "nuxt-vue-2-capi-monorepo-sample",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "format": "prettier --check --ignore-path .gitignore ."
  },
  "devDependencies": {
    "eslint": "^8.3.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-vue": "^8.1.1",
    "prettier": "^2.5.0"
  }
}
