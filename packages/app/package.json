{
  "name": "app",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "prettier --check --ignore-path ../../.gitignore . && eslint --ext \".js,.ts,.vue\" --ignore-path ../../.gitignore ."
  }
}
